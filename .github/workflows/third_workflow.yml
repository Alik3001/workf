name: third Workflow
on: push

jobs:
  run-first-command: 
    runs-on: ubuntu-latest
    steps: 
      - 
        name: List Files and Copy Repo
        run: ls -a
      -
        name: Copy Files
        run: |
          git init
        #uses: actions/checkout@v4
      -
        name: List Files 
        run: ls -a