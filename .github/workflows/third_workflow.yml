name: third Workflow
on: push

jobs:
  run-first-command: 
    runs-on: ubuntu-latest
    steps: 
      - 
        name: Display some text
        run: node -v
      - 
        name: Command in Multiline
        run: | 
          node -v
          npm -v